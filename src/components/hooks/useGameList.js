import React, { useState, useEffect } from 'react'

export default function useGameList(token) {
  const [ gameList, setGameList ] = useState()

  useEffect(() => {
    
  }, [])

  return gameList;
}
